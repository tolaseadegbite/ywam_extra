<%= turbo_stream.prepend "comments", partial: "comments/comment", locals: { comment: @comment } %>
<%= turbo_stream.update "#{dom_id(@commentable)}_comment_form", partial: "comments/form", locals: { commentable: @commentable, comment: Comment.new } %>