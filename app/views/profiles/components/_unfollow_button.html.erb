<%= form_with(model: current_account.active_relationships.find_by(followed: account),
              html: { method: :delete }) do |f| %>
  <%= f.submit "Unfollow", class: "cursor-pointer rounded-full py-2 px-4 rounded bg-blue-600 flex items-center gap-2 text-white hover:bg-blue-700 ml-auto" %>
<% end %>